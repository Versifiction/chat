import React from 'react';
import PropTypes from 'prop-types';

const Messages = () => (
  <div className="app-messages">des messages ici…</div>
);
Messages.propTypes = {};

export default Messages;
